<%= form_with(model: quest, local: true) do |form| %>
  <% if quest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quest.errors.count, "error") %> prohibited this quest from being saved:</h2>

      <ul>
      <% quest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question %>
    <%= form.text_field :question %>
  </div>

  <div class="field">
    <%= form.label :options %>
    <%= form.text_field :options %>
  </div>

  <div class="field">
    <%= form.label :answer_idx %>
    <%= form.number_field :answer_idx %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
